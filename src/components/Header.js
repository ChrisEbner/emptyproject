/* Sample stateless component for the header */

import React from 'react';

import FaMapPin from 'react-icons/lib/fa/map-pin';
const Header = () => {
    return (
        <div>
            <h1>This is the header!</h1>
        </div>
    );
};

export default Header;